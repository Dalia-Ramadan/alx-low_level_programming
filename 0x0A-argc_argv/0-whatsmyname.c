#include "main.h"
#include <stdio.h>
/**
 * @argc: the counter.
 * @argv: is a one-dimensional array of strings.
 */
int main (__attribute__ ((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return 0;
}
