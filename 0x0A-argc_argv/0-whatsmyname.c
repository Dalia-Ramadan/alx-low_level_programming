#include "main.h"
#include <stdio.h>
/**
 * @argc: the counter of the argument
 * @argv: the argument
 */
int main (__attribute__ ((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return 0;
}
