#include"main.h"
#include <stdio.h>
/**
 * argc: the counter
 * argv: the array
 */
int main (int argc, __attribute__ ((unused))char *argv[])
{
	if (argc>1)
	{
	printf("%i\n", argc);
	}
	return (0);
}
